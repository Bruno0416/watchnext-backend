eureka:
  client:
    enabled: false

tmdb:
  api:
    key: "test-key"
    base-url: "https://api.themoviedb.org/3"

application:
  security:
    jwt:
      secret-key: "clave-secreta-falsa-solo-para-entorno-de-pruebas"
      access-token:
        expiration: 86400000

spring:
  data:
    redis:
      host: localhost
      port: 6379

  http:
    codecs:
      max-in-memory-size: 3MB
