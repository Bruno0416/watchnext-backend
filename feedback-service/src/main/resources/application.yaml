server:
  port: 8084

spring:
  application:
    name: feedback-service

  datasource:
    url: ${FEEDBACK_DB_URL:jdbc:postgresql://ep-floral-paper-acot5jab-pooler.sa-east-1.aws.neon.tech:5432/feedback_db?sslmode=require}
    username: ${FEEDBACK_DB_USER}
    password: ${FEEDBACK_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

content-service:
  api:
    base-url: ${CONTENT_SERVICE_BASE_URL:http://content-service:8082/api/v1/content}

application:
  security:
    jwt:
      secret-key: ${JWT_SECRET}

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:${EUREKA_ADMIN_PASSWD}@eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.cloud.client.ip-address}:${server.port}
